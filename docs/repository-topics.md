# Repository Topics

This repository is an AI-powered natural-language-processing learning and teaching platform. The following GitHub Topics are recommended for the repository metadata so that the project is easier to discover and related projects are easier to find:

`agent` · `agent-runtime` · `ai-tutor` · `docker` · `education` · `fastapi` · `langgraph` · `learning-platform` · `llm` · `mcp` · `mysql` · `natural-language-processing` · `nlp` · `observability` · `react` · `redis` · `typescript` · `websocket`

## Maintainer action

After this pull request is reviewed and merged, a repository administrator should apply the list above under **Settings → General → Topics** for the official repository. GitHub Topics are repository metadata and are not transferred by merging a code pull request.

The `agent` topic is intentional; use `agent` rather than `ai-agent`.
