"""Sandbox identity and lifecycle contracts.

Phase 0 deliberately contains no container engine integration.  It defines the
trusted owner/lease boundary that later runtime managers must enforce.
"""
